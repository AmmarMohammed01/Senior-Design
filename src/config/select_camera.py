"""select_camera.py
Stores a variable that will be shared across python scripts to determine which camera routine is run.
The options the user can use to capture an image are either a USB Camera or PiCamera"""
camera_choice = "usb" # default is usb, can be switched to picam by user
